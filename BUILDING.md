# Building the plugin

This repository is intended for GitHub.

Install JDK 21 and Gradle 8.x locally, then run:

    gradle build

If you want a Gradle wrapper committed to GitHub, run:

    gradle wrapper

Then commit `gradlew`, `gradlew.bat` and `gradle/wrapper/`.
